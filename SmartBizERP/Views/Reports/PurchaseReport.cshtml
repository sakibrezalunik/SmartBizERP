@model IEnumerable<SmartBizERP.Models.PurchaseOrder>

@{ ViewBag.Title = "Purchase Report"; }

<h2>Purchase Report</h2>
<p class="text-muted">Received orders only.</p>

<table class="table table-striped table-bordered">
    <thead><tr><th>PO No.</th><th>Supplier</th><th>Date</th><th>Total</th></tr></thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.PurchaseOrderNo</td>
            <td>@item.Supplier.SupplierName</td>
            <td>@item.OrderDate.ToString("yyyy-MM-dd")</td>
            <td>@item.TotalAmount.ToString("N2")</td>
        </tr>
    }
    </tbody>
    <tfoot>
        <tr><th colspan="3" class="text-end">Grand Total</th><th>@(((decimal)ViewBag.GrandTotal).ToString("N2"))</th></tr>
    </tfoot>
</table>

@Html.ActionLink("Back to Reports", "Index", null, new { @class = "btn btn-default" })
